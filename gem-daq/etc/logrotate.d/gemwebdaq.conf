/var/log/gemdaq/web-daq/slot01.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}

/var/log/gemdaq/web-daq/slot02.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}

/var/log/gemdaq/web-daq/slot03.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}

/var/log/gemdaq/web-daq/slot04.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}

/var/log/gemdaq/web-daq/slot05.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}

/var/log/gemdaq/web-daq/slot06.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}

/var/log/gemdaq/web-daq/slot07.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}

/var/log/gemdaq/web-daq/slot08.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}

/var/log/gemdaq/web-daq/slot09.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}

/var/log/gemdaq/web-daq/slot10.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}

/var/log/gemdaq/web-daq/slot11.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}

/var/log/gemdaq/web-daq/slot12.log {
    # Rotate log file weekly, keeping 4 old copies, with rotation date put appended to file name
    weekly
    rotate 4
    dateext

    missingok

    # Create new log file with same permissions as current log file
    create 644

    # Get rsyslogd to close all open files 
    postrotate
      /usr/bin/killall -HUP rsyslogd
    endscript
}
